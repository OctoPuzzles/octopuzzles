{
  "name": "@octopuzzles/database",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "source": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "eslint \"src/**/*.ts*\"",
    "generate": "npx surrealx generate --output src/surrealX.ts",
    "db:migrate": "npx surrealx migrate run",
    "db:reset": "npx surrealx database reset"
  },
  "dependencies": {
    "surrealdb.js": "^0.5.0"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-config-octopuzzles-basic": "*",
    "tsconfig": "*",
    "typescript": "^4.5.3",
    "vitest": "^0.29.2"
  }
}
